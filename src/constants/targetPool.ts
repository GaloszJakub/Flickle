/** Minimum TMDB vote count — filters out obscure titles from list endpoints. */
export const MIN_VOTE_COUNT = 500

/** Top-rated list pages (20 movies each). */
export const TARGET_POOL_PAGES = 5

/** Popular list pages (20 movies each). */
export const POPULAR_POOL_PAGES = 5
